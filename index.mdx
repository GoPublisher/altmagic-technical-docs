---
title: 'Alt Magic API Documentation'
description: 'Generate AI-powered alt text for images with automatic fallback and credit-based pricing'
---

# Alt Magic API

Generate descriptive, SEO-optimized alt text for images using our AI-powered service. The Alt Magic API automatically falls back from Gemini to OpenAI for maximum reliability, ensuring your images always get the alt text they need.

## 🚀 Quick Start

### 1. Get Your API Key

1. Sign up at [app.altmagic.pro](https://app.altmagic.pro)
2. Navigate to **API Keys** section
3. Generate your first API key

[View detailed step-by-step guide →](/api-reference/api-key-generation)

### 2. Make Your First Request

```bash
curl -X POST "https://api.altmagic.pro/api-alt-text" \
  -H "Content-Type: application/json" \
  -H "x-api-key: YOUR_API_KEY" \
  -d '{
    "image_url": "https://example.com/image.jpg",
    "user_id": "your-email@example.com"
  }'
```

### 3. Get Your Alt Text

```json
{
  "altText": "A close-up photo of a red apple on a wooden table",
  "CreditsAvailable": 49,
  "plan_type": "pro"
}
```

## ✨ Key Features

- **🤖 AI-Powered**: Uses Gemini API with automatic OpenAI fallback
- **💳 Credit-Based**: Pay only for what you use
- **⚡ Fast**: Generate alt text in seconds
- **🌍 Multi-Language**: Support for multiple languages
- **🔒 Secure**: API key authentication with rate limiting
- **📱 Easy Integration**: Simple REST API with comprehensive SDKs

## 🛠️ API Endpoints

### Alt Text Generation

**`POST /api-alt-text`**

Generate descriptive alt text for any image with optional context parameters.

[View Full Documentation →](/api-reference/alt-text-generation)

## 🔐 Authentication

The Alt Magic API supports two authentication methods:

- **API Key**: Include `x-api-key` header for server-to-server integrations
- **Session**: Use existing user sessions for web applications

[Learn More →](/api-reference/authentication)

## 📊 Rate Limits

- **Standard Plan**: 100 requests per minute
- **Pro Plan**: 500 requests per minute
- **Enterprise Plan**: Custom limits

[View Details →](/api-reference/rate-limits)

## 🚨 Error Handling

Comprehensive error handling with detailed response codes and retry logic support.

[Learn More →](/api-reference/error-handling)

## 💰 Pricing

### Credit System

- **1 Credit** = **1 Alt Text Generation**
- **Bulk Discounts** available for high-volume users
- **No Monthly Fees** - pay only for what you use

### Plans

| Plan | Credits | Price | Rate Limit |
|------|---------|-------|------------|
| **Starter** | 100 | $9.99 | 100/min |
| **Pro** | 1,000 | $49.99 | 500/min |
| **Enterprise** | Custom | Custom | Custom |

## 🔧 SDKs & Libraries

### JavaScript/Node.js

```bash
npm install alt-magic-api
```

```javascript
import { AltMagicAPI } from 'alt-magic-api';

const api = new AltMagicAPI('YOUR_API_KEY');
const result = await api.generateAltText({
  image_url: 'https://example.com/image.jpg',
  user_id: 'user@example.com'
});
```

### Python

```bash
pip install alt-magic-api
```

```python
from alt_magic_api import AltMagicAPI

api = AltMagicAPI('YOUR_API_KEY')
result = api.generate_alt_text({
    'image_url': 'https://example.com/image.jpg',
    'user_id': 'user@example.com'
})
```

### cURL

```bash
curl -X POST "https://api.altmagic.pro/api-alt-text" \
  -H "Content-Type: application/json" \
  -H "x-api-key: YOUR_API_KEY" \
  -d '{
    "image_url": "https://example.com/image.jpg",
    "user_id": "user@example.com"
  }'
```

## 📚 Examples

### Basic Usage

Generate alt text for a simple image:

```javascript
const response = await fetch('https://api.altmagic.pro/api-alt-text', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'x-api-key': API_KEY
  },
  body: JSON.stringify({
    image_url: 'https://example.com/product.jpg',
    user_id: 'user@example.com'
  })
});
```

const result = await response.json();
console.log(result.altText);
// Output: "A modern smartphone displayed on a white background"
```

### With Context

Provide additional context for better alt text:

```javascript
const response = await fetch('https://api.altmagic.pro/api-alt-text', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'x-api-key': API_KEY
  },
  body: JSON.stringify({
    image_url: 'https://example.com/recipe.jpg',
    blog_title: 'Homemade Pizza Recipe',
    seo_keywords: 'pizza, cooking, recipe, homemade',
    language_code: 'en',
    user_id: 'user@example.com'
  })
});
```

### Batch Processing

Process multiple images efficiently:

```javascript
const imageUrls = [
  'https://example.com/image1.jpg',
  'https://example.com/image2.jpg',
  'https://example.com/image3.jpg'
];

const results = await Promise.all(
  imageUrls.map(url => 
    api.generateAltText({
      image_url: url,
      user_id: 'user@example.com'
    })
  )
);

results.forEach((result, index) => {
  console.log(`Image ${index + 1}: ${result.altText}`);
});
```

## 🎯 Use Cases

### E-commerce
- Product image descriptions
- Category page optimization
- Search result enhancement

### Content Marketing
- Blog post images
- Social media content
- Newsletter graphics

### Accessibility
- Screen reader support
- WCAG compliance
- Inclusive design

### SEO Optimization
- Image search optimization
- Alt text for better rankings
- Contextual descriptions

## 🔍 Best Practices

### Image URLs
- Ensure images are publicly accessible
- Use HTTPS URLs when possible
- Avoid authentication-protected images

### Rate Limiting
- Implement exponential backoff
- Queue requests for high-volume usage
- Monitor your usage patterns

### Error Handling
- Always check response status codes
- Implement retry logic for transient errors
- Log errors for debugging

### Security
- Store API keys securely
- Use environment variables
- Rotate keys regularly

## 📞 Support

### Documentation
- **API Reference**: Complete endpoint documentation
- **Code Examples**: Ready-to-use code snippets
- **SDK Documentation**: Language-specific guides

### Getting Help
- **Email**: support@altmagic.pro
- **Status Page**: [status.altmagic.pro](https://status.altmagic.pro)
- **Community**: [community.altmagic.pro](https://community.altmagic.pro)

### Resources
- **Quick Start Guide**: Get up and running in minutes
- **API Playground**: Test endpoints interactively
- **Changelog**: Track API updates and improvements

## 🚀 Ready to Get Started?

1. **[Sign Up](https://app.altmagic.pro/signup)** for a free account
2. **[Generate API Key](https://app.altmagic.pro/api-keys)** in your dashboard
3. **[Try the API](https://playground.altmagic.pro)** in our interactive playground
4. **[Read the Docs](/api-reference/alt-text-generation)** for detailed information

---

*Alt Magic API - Making images accessible, one alt text at a time.*
